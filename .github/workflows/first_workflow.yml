name: Mon premier Workflow

on:
  push:
     branches:  [ main ]


jobs:
   hello-world:
      runs-on: ubuntu-latest


     steps:
       - name: Dire bonjour
         run: echo "Hello, World! CI/CD avec Github Actions"

       - name: Affiche la date
         run: date


       - name: lister  les fichiers
         run: ls -la


